# Phase 5.2: 统计增强

> **目标**: 词库页展示更丰富的 SRS 学习信息，新增学习统计和困难词标记  
> **创建日期**: 2026-02-15  
> **依赖**: Phase 5.1 ✅

---

## 1. 词库页展开详情 — SRS 信息条

### 需求
在词库页展开单词详情时，底部显示该单词的 SRS 复习信息。

### UI 设计

```
hello  ⭐☆☆  简洁中文释义
━━━━━━━━━━━━━━━━━━━━━━━━━━
/həˈloʊ/  int.  🔊
This is an example. 🔊
这是一个例子。

📖 复习 12 次 | 🔥 连对 1 次
📅 上次 02-14 | ⏰ 下次 02-17

[标为已掌握]  [删除]
```

### 四个指标

| 指标 | 字段 | 显示格式 | 说明 |
|------|------|---------|------|
| 📖 复习次数 | `totalReviews` | `复习 N 次` | 0 次显示 `复习 0 次` |
| 🔥 连续答对 | `correctStreak` | `连对 N 次` | 体现当前势头 |
| 📅 上次复习 | `lastReviewedAt` | `上次 MM-DD` | null 显示 `上次 --` |
| ⏰ 下次复习 | `nextReviewDate` | `下次 MM-DD` | null（新词）显示 `下次 待定`；mastered 显示 `已掌握` |

### 样式要求
- 两行排列，竖线分隔
- 字体小一号（0.8rem），颜色 `#888`
- 与现有详情内容之间加分隔线
- class: `.srs-info`

### 代码改动
- `app.js` — `renderLibrary()` 函数中，在 `.lib-detail` 的 `.lib-actions` 之前插入 SRS 信息 HTML
- `style.css` — 新增 `.srs-info` 样式

---

## 2. 学习统计

### 需求
设置页底部新增"学习统计"区域，展示整体学习情况。

### 统计项

| 统计 | 计算方式 | 显示 |
|------|---------|------|
| 总词数 | `getAllCards().length` | `总词数：30` |
| Level 分布 | 按 level 0-3 + mastered 分组 | `新词 5 · 初识 8 · 熟悉 10 · 巩固 5 · 掌握 2` |
| 今日进度 | 从 `todayReview` 读取 | `今日：8/10（答对 6，答错 2）` |
| 连续学习天数 | localStorage `studyStreak` | `连续学习：5 天 🔥` |

### 连续学习天数逻辑
```javascript
// localStorage: studyStreak = { lastDate: "2026-02-14", count: 5 }
// 每次完成至少一道题时更新：
//   - lastDate === 今天 → 不变
//   - lastDate === 昨天 → count + 1
//   - 其他 → count = 1
```

### UI 位置
在设置页 `settings-stats` 下方，替换原有的简单统计文字。

---

## 3. 困难词标记

### 需求
在词库页标记"困难词"——复习次数多但 level 仍然低的单词。

### 判定规则
```javascript
function isDifficult(card) {
  return card.totalReviews >= 6 && card.level <= 1 && !card.mastered;
}
```
- 复习 ≥6 次但还在 level 0-1 → 说明老是记不住

### 显示
- 词库列表中，困难词的 word 后面加 `🔴` 标记
- 展开详情的 SRS 信息条中显示 `⚠️ 困难词`

---

## 4. 测试用例

### tests/srs.test.js 新增
无需新增（统计逻辑简单，主要是 UI 渲染）

### 手动验证清单
- [ ] 展开词库单词，SRS 信息正确显示
- [ ] 新词显示 `复习 0 次 | 连对 0 次 | 上次 -- | 下次 待定`
- [ ] 已掌握的词显示 `⏰ 已掌握`
- [ ] 设置页统计数字正确
- [ ] 连续学习天数计算正确
- [ ] 困难词标记显示

---

## 5. 实现优先级

1. **词库页 SRS 信息条**（最直接的需求）
2. **学习统计**（设置页增强）
3. **困难词标记**（锦上添花）

---

**预计工作量**: Opus sub-agent 1 轮
